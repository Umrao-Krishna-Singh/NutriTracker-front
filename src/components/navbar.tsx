import { Button } from './ui/button'

const navbar = () => {
    return (
        <>
            <Button variant={'default'}>Click Me!</Button>
            <div>Navbar</div>
        </>
    )
}

export default navbar
