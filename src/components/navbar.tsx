import { Button } from './ui/button'

const navbar = () => {
    return (
        <>
            <Button variant={'outline'}>Click Me!</Button>
            <div>Navbar</div>
        </>
    )
}

export default navbar
